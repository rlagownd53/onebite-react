import mul, { add, sub } from "./math.js";

//라이브러리를 불러올 땐 뒤에 경로가 아니라 그냥 이름만 명시
import randomColor from "randomcolor";

const color = randomColor();
console.log(color);
//console.log(add(1, 2));
//console.log(sub(1, 2));
//console.log(mul(2, 3));
